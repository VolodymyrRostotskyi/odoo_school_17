<odoo>

    <record id="view_hr_hospital_patient_wizard_form" model="ir.ui.view">
        <field name="name">hr.hospital.patient.wizard.form</field>
        <field name="model">hr.hospital.patient.wizard</field>
        <field name="arch" type="xml">
            <form string="Change Doctor for Patients">
                <group>
                    <field name="doctor_id"/>
                    <field name="patient_ids" readonly="1"/>
                </group>
                <footer>
                    <button name="action_change_doctor" type="object"
                            string="Apply" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary"
                            special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="hr_hospital_set_personal_doctor_wizard_act_window"
            model="ir.actions.act_window">
        <field name="name">Set Personal Doctor</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">hr.hospital.patient.wizard</field>
        <field name="view_mode">form,tree</field>
        <field name="target">new</field>
        <field name="binding_model_id"
               ref="hr_hospital.model_hr_hospital_patient"/>
    </record>

</odoo>
